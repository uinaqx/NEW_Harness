# 某科学的Agent 使用说明

某科学的Agent 是一个**本地个人使用**的 Windows 桌面助手。Work 模式的任务由内置的 **OpenCode 引擎**执行，
界面提供实时执行画布、授权交互与文件差异查看；Chat 模式可直接开聊，无需项目，且不开放本地文件或工具能力。默认中文界面、单 Agent、深色/浅色单色主题。

> 不需要 Codex / Cline / OpenCode 云端账户，也不依赖你预先安装 Bun、Node 或 OpenCode。

---

## 1. 安装

1. 从 GitHub Release 下载并运行 `New-Harness_<版本>_x64-setup.exe`（本地构建产物的文件名为 `某科学的Agent_<版本>_x64-setup.exe`，内容相同）。
2. 安装程序会：
   - 先退出正在运行的本应用及其子进程（后端与执行引擎）；
   - 如检测到关键文件被占用，会**中止安装并提示**，而不是忽略后继续；
   - 按需安装 **Microsoft WebView2 运行时**（目标机缺失时自动下载，需联网）；
   - 保留你已有的会话与设置（数据在用户数据目录，不在安装目录）。
3. 此后升级 0.4.0 及更新版本可直接覆盖。由于 0.4.0 更改了产品名，旧版 `Harness` 会暂时并存；请启动新名称的应用。旧版安装目录同时存放了沿用的数据目录，清理旧版前请先备份 `%LOCALAPPDATA%\Harness\data`，不要直接删除整个目录。

安装目录内容：

```
某科学的Agent/
  harness-shell.exe            桌面外壳（内含后端可执行文件，首次启动释放到数据目录）
  opencode/opencode.exe        执行引擎（固定版本，见 opencode/PIN.json）
  opencode/LICENSE             引擎许可证
  opencode/PIN.json            引擎版本与校验值
  skills/                     20 个内置 Agent Skills（含来源与许可证）
  THIRD-PARTY-NOTICES.md       第三方组件与许可证
  HARNESS.md                   本文件
```

## 2. 首次配置

点击左下角 **设置**，填写：

| 项目 | 说明 |
| --- | --- |
| API 配置 | 可保存多份具名配置；每份选择 `OpenAI 兼容 · Chat Completions` 或 `Anthropic · Messages` |
| Base URL | 例如 `https://api.openai.com/v1`、`https://api.anthropic.com`。结尾多余的斜杠会被去掉，路径前缀原样保留 |
| 模型 ID | 每份 API 配置可填写多个模型 ID（每行一个）；在输入框右下角切换配置与模型，不依赖远端模型列表 |
| API Key | 各配置的 Key 使用 **Windows DPAPI** 分别加密保存，界面只显示掩码 |
| 工作区目录 | 新对话输入框里选择项目文件夹，也可在设置里填写绝对路径作为默认值。项目内读取默认允许；**修改文件与执行命令默认需要授权** |
| 自动批准 | 可选择自动批准文件修改、命令执行；默认均为关闭。实际是否弹窗由固定版本 OpenCode 的权限判定决定，见第 8 节 |
| 外观主题 | 侧栏底部可随时切换深色/浅色；设置弹窗也有同一选项，选择会保存，重启后保持 |

**「保存配置」与「测试连接」是两个独立操作**：

- 测试连接会发送一个最小、无工具调用的真实请求（20 秒超时），并区分提示：
  Key 无效、模型不存在、协议不兼容、网络失败、超时。
- 运行任务期间修改配置不会打断当前任务，**从下一轮对话开始生效**。

## 3. 使用

1. 左侧点「Work 新对话」会立即打开空白对话；在输入框选择访问位置和模型后发送第一条消息，才会创建会话。也可先「新建项目」，在同一文件夹下建立多个对话。
   点「Chat 开聊」则直接进入纯对话，无需选择访问位置；Chat 对话固定排列在左侧项目列表下方。
2. 在输入框描述任务，`Enter` 发送，`Shift+Enter` 换行。右下角可切换已配置 API 下的模型。
   输入框左下角 `+` 可附加文件/文件夹、设置该对话的持续目标、切换计划模式，或选择 20 个内置 Agent Skills。计划模式使用 OpenCode 的 `plan` agent。选择技能后，引擎会在本轮提示模型通过原生 `skill` 工具加载。
   Chat 没有附件、技能、计划模式、执行画布或差异入口，也不提供本地文件读写、命令执行能力。
   侧栏对话右键或点 `···` 可重命名、置顶和删除；项目名称也可更改。模型、计划模式与目标按会话保存。
3. **执行画布**在每轮开始时展开（位于聊天内容下方、输入框上方）：
   - 每个工具调用是一个节点，显示工具名与状态（执行中 / 等待授权 / 已结束 / 失败 / 已中断）；
   - 命令节点会显示引擎报告的**退出码**，非零退出码按失败处理；
   - 画布高度不超过内容区的 40%，宽度不足时按宽度淘汰最旧的已结束节点并关闭其详情；
   - **执行中与等待授权**的节点不会被淘汰；
   - 一轮结束后停留约 1.2 秒，再用约 300ms 收起；
   - 遵循系统「减少动态效果」设置。
4. **授权**：引擎请求授权时，画布底部出现授权卡片，显示可读的操作说明（文件路径或完整命令），
   可点 **允许本次** 或 **拒绝**。拒绝后由引擎记录该工具调用失败，界面不会伪造结果。
   授权等待期间点击「停止」会立即结束等待。
5. **差异**：标题栏「差异」打开右侧面板，按文件列出增删行数与补丁文本；
   二进制文件、被删除文件与无差异情况都会给出明确状态。
6. **停止任务**：先调用引擎的取消接口；若引擎在 8 秒内未响应，会强制终止本应用所属进程树，
   并明确提示「任务已中断，不会自动重发」。
7. 一个应用实例**同一时间只允许一个主动执行任务**；切换会话不会停止任务。
   正在执行的会话不能直接删除，需先停止。
8. 关闭窗口即退出应用（不会驻留后台）。

## 4. 数据位置与隐私

```
%LOCALAPPDATA%\Harness\data\
  app-settings.json     非敏感设置（API 列表、Base URL、模型、主题、上次工作区）
  credentials.bin       默认 API 的 DPAPI 加密 Key（同一 Windows 用户 + 同一台机器才可解密）
  credentials\          其他 API 的 DPAPI 加密 Key
  runtime.json          启动握手（端口、一次性令牌、实例 ID）；退出时删除
  sessions\index.json   Work 项目和 Chat 会话索引——不含消息正文
  chat-workspace\       Chat 引擎的私有空目录，不在侧栏作为项目展示
  sessions\legacy\      旧版手写 Agent 的只读历史归档
  opencode\engine\harness-engine.exe   实际运行的引擎副本（从安装目录复制而来）
  opencode\             引擎的隔离数据目录与配置目录
  logs\backend.log      后端日志（脱敏）
  logs\shell.log        外壳日志（启动阶段、退出码）
  logs\engine.log       引擎日志（脱敏）
```

继续沿用 `%LOCALAPPDATA%\Harness\data` 这个内部数据目录，是为了升级时保留旧会话与配置；产品显示名称和安装目录已更改。

安装目录中的 `opencode\opencode.exe` 只作为**分发与校验源**，运行时不会被锁定：
后端会把它复制成数据目录下的 `harness-engine.exe` 并从那里启动。
这样覆盖升级可以随时替换安装目录里的文件，安装器也能用固定的进程名安全地结束引擎
（不会误杀你自己安装的 `opencode.exe`）。

- **API Key 不会出现在**会话文件、普通配置、日志或错误提示中；界面读取设置只返回「是否已配置」与掩码。
- Key 通过子进程环境变量传给引擎（`OPENCODE_CONFIG_CONTENT`），**不写入磁盘配置文件**，
  也不调用会把凭证落盘的引擎接口。
- 会话、消息与工具记录由引擎保存；Harness 不再维护第二套消息库，也不做双向同步。

## 5. 网络与端口

- 后端与引擎**只监听 `127.0.0.1`**，端口由操作系统动态分配（不固定 3126 / 4096）。
- 启动握手通过 `runtime.json` 传递实际地址；前端只连接 Harness 后端，不持有引擎凭证。
- 后端启动时生成一次性随机令牌；WebSocket 必须携带该令牌并通过来源校验才能连接。
- 引擎本地服务启用**随机用户名 + 随机密码**的 Basic 认证。
- 健康检查同时校验版本与实例身份：端口能连上并不等于连到了本应用启动的服务。

## 6. 故障排查

| 现象 | 处理 |
| --- | --- |
| 启动后显示「执行引擎未就绪」 | 点该页面的 **查看诊断** 读取引擎日志与环境信息，再点 **重试启动** |
| 提示引擎哈希校验失败 | 重新运行 `bun scripts/fetch-opencode.mjs --force`，或用 `HARNESS_OPENCODE_BIN` 指向受信任的 `opencode.exe`；如确认自担风险可设 `HARNESS_ALLOW_UNPINNED_ENGINE=1` |
| 提示「无法连接模型服务」 | 检查 Base URL 与网络；用「测试连接」区分是 Key、模型还是网络问题 |
| 命令一直不返回 | 点「停止」；若提示已强制终止，说明引擎未响应取消，任务已中断且不会自动重发 |
| 端口被占用 | 无需处理：端口由系统分配，且会校验实例身份 |
| 想彻底重置 | 退出应用后删除 `%LOCALAPPDATA%\Harness\data`（会丢失会话与设置） |

## 7. 从源码构建

前置：Bun ≥ 1.3、Node ≥ 22、Rust ≥ 1.85、网络可用。

```bash
cd harness
bun install                     # 依赖（不提供 npm/yarn/pnpm 锁文件）
bun scripts/fetch-opencode.mjs  # 拉取固定版本引擎并按 SHA-256 校验
bun run build                   # 前端 + 后端 + 外壳 + NSIS 安装包
bun scripts/package-source.mjs  # 生成源码快照
```

构建产物：

- `shell/src-tauri/target/release/bundle/nsis/*.exe` —— 安装包
- `delivery/<版本>/某科学的Agent_<版本>_x64-setup.exe`、`delivery/<版本>/某科学的Agent-<版本>-source.zip`、`delivery/<版本>/SHA256SUMS.txt`

构建脚本不包含任何个人机器绝对路径，也不依赖特定 shell 的环境变量改写；
`tauri` CLI 通过 `node_modules/@tauri-apps/cli` 解析，也可用 `HARNESS_TAURI_CLI` / `HARNESS_NODE` 指定。

### 开发模式

```bash
# 终端 1：后端（HARNESS_DEV=1 允许开发用前端免读取握手文件获取令牌）
cd harness/backend && HARNESS_DEV=1 bun run src/index.ts

# 终端 2：前端
cd harness/webview && bun run dev      # http://127.0.0.1:3125
```

### 自动化验收

```bash
cd harness
bun run verify:phase1       # 引擎最小接入（本地模拟模型服务）
bun run verify:integration  # 端到端（真实后端子进程 + WS 协议 + 模拟模型服务）
```

> 两个脚本都使用**本地模拟模型服务**，不能替代真实模型验收；结论与未完成项见 `VALIDATION.md`。

## 8. 已知限制

详见 `VALIDATION.md` 的「已知限制」章节，摘要：

- 引擎在**同一会话内重复执行同类命令**时不一定会再次弹出授权（已实测记录）；界面仍按引擎的原生权限机制展示与回复。
- 文件差异依赖工作区是 git 仓库；非 git 目录下引擎可能返回空差异。
- 真实模型连接需要你自备凭证；仓库内没有可用于验收的凭证，因此该项标记为未执行。
- 构建机若 Bun 装在非 ASCII 路径下，`bun build --compile` 需要脚本自动复制到 ASCII 目录（已内置处理）。
